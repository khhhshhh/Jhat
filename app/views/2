extends layout

block head

block content
    ul
    -friends.forEach(function(friend) {
        li=user 
            button.add(value=friend._id) remove friend
            br
            br
    -});

block script
    script(type='text/javascript', src='/js/friendSeeking.js')
